let positiveNumbers = [1, -3, 5, -3, 0]
let numbers = positiveNumbers.forEach(number => {
     if(number >= 0) {
         console.log(number)
     }
 })

 let positiveNumbers = [1,2,3]
 let numbers = positiveNumbers.forEach(number => {
     if(number >= 0) {
         console.log(number)
     }
 })

 let positiveNumbers = [-1,-2,-3]
 let numbers = positiveNumbers.forEach(number => {
     if(number >= 0) {
         console.log(number)
     }else{
         console.log([])
     }
 })
